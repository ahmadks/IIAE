# Quick Start: IIAE for Junior Developers

**Time to first working example: 10 minutes**

---

## Installation (2 minutes)

```bash
# Clone & enter directory
git clone <repo>
cd IIAE

# Create virtual environment
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Install IIAE
pip install -e .

# Verify
python -c "from iiae import validate; print('✓ IIAE ready')"
```

... (document continues)
